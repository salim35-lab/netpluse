# سوشيال كونكت (Social Connect)

منصة تواصل اجتماعي شاملة تتيح للمستخدمين التواصل ومشاركة المحتوى والتفاعل مع الآخرين.

## 🌟 المميزات الرئيسية

### 📱 الصفحة الرئيسية
- عرض آخر المنشورات من المستخدمين المتابَعين
- إمكانية نشر محتوى جديد (نصوص وصور)
- التفاعل مع المنشورات (إعجاب، تعليق، مشاركة)

### 🔍 البحث
- البحث عن مستخدمين بالاسم
- البحث عن منشورات بالكلمات المفتاحية
- عرض نتائج البحث بشكل منظم

### 🌐 الاستكشاف
- عرض المنشورات الشائعة والمميزة
- اقتراحات لمستخدمين جدد للمتابعة

### 🔔 الإشعارات
- إشعارات الإعجابات والتعليقات
- إشعارات المتابعة الجديدة
- إشعارات الإشارات في المنشورات

### 💬 الرسائل
- إرسال واستقبال رسائل خاصة
- عرض قائمة المحادثات
- إشعارات الرسائل الجديدة

### 👤 الملف الشخصي
- عرض معلومات المستخدم الأساسية
- عرض منشورات المستخدم
- إحصائيات (عدد المتابعين، المتابَعين، المنشورات)
- إمكانية تعديل الملف الشخصي
- زر المتابعة/إلغاء المتابعة

## 🎨 التصميم

- **اللون الرئيسي**: الأزرق الفاتح (#1DA1F2)
- **التخطيط**: تصميم ثلاثي الأعمدة
  - قائمة جانبية يسرى للتنقل
  - محتوى رئيسي في الوسط
  - اقتراحات في العمود الأيمن
- **الأيقونات**: أيقونات خطية بسيطة
- **البطاقات**: حواف دائرية مع ظل خفيف
- **متجاوب**: يعمل على جميع الأجهزة (Desktop, Tablet, Mobile)

## 🛠️ التقنيات المستخدمة

- **Frontend**: React + TypeScript + Vite
- **UI Framework**: shadcn/ui + Tailwind CSS
- **Backend**: Supabase (Database + Storage + Auth)
- **State Management**: React Context API
- **Routing**: React Router v6
- **Form Handling**: React Hook Form + Zod

## 📦 البنية التقنية

```
src/
├── components/
│   ├── layouts/          # مكونات التخطيط
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   ├── RightSidebar.tsx
│   │   └── MainLayout.tsx
│   ├── common/           # مكونات مشتركة
│   │   ├── PostCard.tsx
│   │   ├── CreatePost.tsx
│   │   └── RouteGuard.tsx
│   └── ui/               # مكونات UI من shadcn
├── pages/                # صفحات التطبيق
│   ├── HomePage.tsx
│   ├── LoginPage.tsx
│   ├── ProfilePage.tsx
│   ├── SearchPage.tsx
│   ├── ExplorePage.tsx
│   ├── NotificationsPage.tsx
│   └── MessagesPage.tsx
├── contexts/             # Context API
│   └── AuthContext.tsx
├── db/                   # قاعدة البيانات
│   ├── supabase.ts
│   └── api.ts
├── types/                # أنواع TypeScript
│   └── types.ts
└── hooks/                # Custom Hooks

```

## 🗄️ قاعدة البيانات

### الجداول:
- **profiles**: معلومات المستخدمين
- **posts**: المنشورات
- **comments**: التعليقات
- **likes**: الإعجابات
- **follows**: المتابعات
- **messages**: الرسائل الخاصة
- **notifications**: الإشعارات

### التخزين:
- **Supabase Storage**: لتخزين الصور
- ضغط تلقائي للصور أكبر من 1MB
- تحويل إلى WEBP للحفاظ على الجودة

## 🔐 نظام المصادقة

- تسجيل الدخول باستخدام اسم المستخدم وكلمة المرور
- أول مستخدم يسجل يصبح مسؤولاً (Admin) تلقائياً
- حماية الصفحات بنظام RouteGuard
- إدارة الجلسات باستخدام Supabase Auth

## 🚀 البدء

### المتطلبات:
- Node.js 18+
- pnpm

### التثبيت:
```bash
pnpm install
```

### التشغيل:
```bash
pnpm dev
```

### البناء:
```bash
pnpm build
```

## 📝 ملاحظات مهمة

1. **المسؤول الأول**: أول مستخدم يسجل في المنصة سيكون مسؤولاً تلقائياً
2. **رفع الصور**: يتم ضغط الصور تلقائياً إذا كانت أكبر من 1MB
3. **أسماء الملفات**: يجب أن تحتوي على أحرف إنجليزية وأرقام فقط
4. **اسم المستخدم**: يمكن استخدام الأحرف الإنجليزية والأرقام و _ فقط

## 🔒 الأمان

- Row Level Security (RLS) مفعل على جميع الجداول
- المستخدمون يمكنهم فقط تعديل بياناتهم الخاصة
- المسؤولون لديهم صلاحيات حذف أي محتوى
- حماية من SQL Injection عبر Supabase

## 📱 التوافق

- ✅ Desktop (1920x1080, 1366x768, 1440x900)
- ✅ Laptop (1280x720, 1536x864)
- ✅ Mobile (375x667, 414x896, 430x932)

## 🎯 الميزات المستقبلية

- [ ] إضافة الهاشتاجات
- [ ] إضافة الإشارات (@mentions)
- [ ] إضافة الإشعارات الفورية
- [ ] إضافة القصص (Stories)
- [ ] إضافة البث المباشر
- [ ] إضافة المجموعات

## 📄 الترخيص

© 2025 سوشيال كونكت - جميع الحقوق محفوظة
